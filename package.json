{
  "name": "werkdeutsch-backend",
  "version": "1.0.0",
  "private": true,
  "comment": "This package.json exists only for the amplify/ backend (DynamoDB + Lambda, deployed via CDK during Amplify's backend build phase). The frontend (index.html, app.js, etc.) stays dependency-free and is never built — see amplify.yml.",
  "scripts": {
    "sandbox": "ampx sandbox"
  },
  "devDependencies": {
    "@aws-amplify/backend": "^1.14.0",
    "@aws-amplify/backend-cli": "^1.4.0",
    "@types/node": "^20.14.0",
    "aws-cdk-lib": "^2.160.0",
    "constructs": "^10.3.0",
    "typescript": "^5.5.0",
    "tsx": "^4.19.0"
  }
}
